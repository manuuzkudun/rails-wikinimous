<%= link_to "Create a new article", new_article_path %>

<ul>
  <% @articles.each do |article| %>
    <li>
      <h3><%= link_to article_path(article) do %>
          <%= article.title %>
          <% end %>
      </h3>
      <p><%= article.content %></p>
      </li>
  <% end %>
</ul>
